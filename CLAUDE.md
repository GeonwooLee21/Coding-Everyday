# CLAUDE.md — C++ 자료구조 일일 문제 생성 루틴

## 프로젝트 목적 (Purpose)

이 레포는 **C++ 언어 숙련도**와 **자료구조 복습**을 동시에 끌어올리기 위한
일일 학습 공간이다. 매일 자료구조 개념 하나를 다루는 문제를 생성하고,
사용자는 제공된 스켈레톤 코드의 빈칸(TODO)을 채우며 학습한다.

- 핵심 병목은 자료구조 개념이 아니라 **C++ 문법 숙련도**다.
  따라서 문제는 "설계"보다 "구현 반복 노출"에 무게를 둔다.
- 자료구조 로직의 뼈대는 스켈레톤이 잡아주고,
  사용자는 문법·연산자·STL 사용에 집중할 수 있게 한다.

---

## 레포 구조 (Repository Layout)

- `daily-learning/cpp/` — 자료구조 구현 학습
  - `problems/DayNN_<topic>_<variant>.cpp` — TODO 빈칸이 있는 스켈레톤 코드
  - `markdown/DayNN_<topic>_<variant>.md` — 문제 개요(overview) + 하단에 사용자가
    직접 작성하는 "배운 점 / 정리" 섹션
  - `NN`은 순차 증가하는 2자리 번호다 (예: `01`, `02`, `03`...). **오늘이 Day01이다.**
    "day" 접두사는 파일명에 `Day`로 붙인다 (예: `Day01_stack_array`).
  - `<variant>`는 같은 주제의 변형/단계를 나타낸다 (예: `array`, `linked`, `applied`).
- 기존 폴더 `01_stack/`, `02_list/`, `03_stack/`은 **건드리지 않는다.**
  과거 방식으로 만들어진 것이며, 새 문제부터 위의 파일 기반 구조를 쓴다.
- `algorithm/cpp/` — 프로그래머스/Codeforces 문제 풀이 (이 루틴과 별개)

> ⚠️ 매일 새 폴더를 만들지 않는다. `problems/`와 `markdown/` 두 폴더 안에
> `DayNN_*` 파일만 짝지어 추가한다 (python 폴더와 동일한 방식).

---

## 일일 문제 포맷 (Daily Problem Format)

매일 같은 이름(`DayNN_<topic>_<variant>`)으로 짝지어 두 파일을 만든다:
`markdown/DayNN_*.md` 와 `problems/DayNN_*.cpp`.

### 1. `markdown/DayNN_*.md` — 문제 개요 + 정리 공간

다음 섹션을 순서대로 포함한다 (한/영 병기):

- 📌 **학습 목표 (Learning Goals)** — 오늘 다루는 자료구조와 관련 C++ 문법
- 📖 **개념 설명 (Concept)** — 해당 자료구조가 무엇이고 왜 쓰는지 3~5줄
- 🎯 **미션 (Mission)** — 무엇을 구현해야 하는지
- 📋 **요구사항 (Requirements)** — 구현할 메서드/멤버 함수 목록 (표 형식)
- 💡 **예제 (Examples)** — 입출력 예시 코드 블록
- 🤔 **생각해보기 (Think About It)** — 개념 이해를 돕는 질문 2~3개
- 📝 **배운 점 / 정리 (My Notes)** — **빈 섹션으로 둔다.** 사용자가 문제를 풀고
  직접 실수·깨달음을 기록하는 공간. 이 부분은 절대 채우지 않는다.

### 2. `problems/DayNN_*.cpp` — 스켈레톤 코드

- 클래스/구조체의 뼈대는 제공하되, 핵심 로직은 `// TODO N:` 주석과
  빈칸(`/* ___ */`)으로 비워 둔다.
- TODO에는 **한/영 병기 설명**과 필요 시 **힌트(Hint)**를 단다.
- 파일 하단에 **테스트 섹션(`int main()`)을 포함**하며,
  이 부분은 `// 🎪 테스트 영역 (수정 금지! / DO NOT MODIFY!)`로 표시하고
  `assert`로 정답을 검증할 수 있게 한다.
- 컴파일 한 번에 통과하도록, 사용자가 채울 부분 외의 코드는 완결되어 있어야 한다.

---

## 진행 방식 & 난이도 곡선 (Progression & Difficulty)

**방식 A — 주제를 순서대로 한 바퀴, 난이도는 각 주제 안에서 상승.**

- 하루 한 주제(변형)씩, 커리큘럼 순서대로 진행한다.
- 한 자료구조는 여러 날에 걸쳐 다루며, **그 주제 안에서** 난이도가 올라간다.
  같은 문법이 손에 익을 때까지 연속 노출하는 것이 목적이다.
- 예시 (Stack의 경우, 폴더 번호가 아니라 주제 내부 진행 단계):
  - 1단계: 배열 기반, 빈칸 촘촘하게 (한 줄 단위 — 연산자·인덱스·STL 호출 하나하나)
  - 2단계: 링크드 기반, 빈칸 함수 단위로 (조건 분기·루프 전체)
  - 3단계: 응용/변형, 빈칸 메서드 단위로 (시그니처만 주고 본문 비우기)
- 한 주제를 며칠 다룰지는 사용자의 소화 속도에 맞춘다.
  막힘 없이 잘 풀면 다음 주제로 넘기고, 실수가 잦으면 같은 주제에서 하루 더.

> 전체 커리큘럼(8개 주제)을 한 바퀴 도는 데 대략 5~6주가 걸리며,
> 그 이후는 스켈레톤 없이 전체 직접 구현으로 반복하며 정착시킨다.

> 사용자가 최근 며칠간 특정 문법에서 반복 실수했다면,
> 그 문법을 다시 노출하는 문제를 우선 배치한다.

---

## 커리큘럼 순서 (Curriculum Order)

아래 순서로 **한 주제씩** 진행하며, 각 주제는 여러 날에 걸쳐
난이도를 올려가며 다룬다 (위 "진행 방식" 참고). 사용자의 진도·요청에 따라 조정한다.

완료 여부는 아래 체크박스로 추적한다. 한 주제가 "진도 판단 규칙"의 기준을
충족하고 사용자 승인을 받으면 `[ ]`를 `[x]`로 바꾼다.

- [ ] 1. Stack (배열 기반 → 링크드 기반 → 응용)
- [ ] 2. Queue (배열 기반 → 원형 큐 → 링크드 기반)
- [ ] 3. Linked List (단일 → 이중)
- [ ] 4. Deque
- [ ] 5. Hash Table (체이닝 → 개방 주소법)
- [ ] 6. Binary Tree / BST
- [ ] 7. Heap (Priority Queue)
- [ ] 8. Graph (인접 리스트 → 인접 행렬)

각 자료구조마다 STL 대응물(`std::stack`, `std::queue`, `std::unordered_map` 등)을
직접 구현 후 비교하는 단계를 마지막 날에 포함한다.

> 현재 진행 중인 주제는 가장 위의 미완료(`[ ]`) 항목이다.
> 다음에 어떤 주제·변형을 낼지는 항상 직전까지의 진도를 확인하고 결정한다.

---

## 진도 판단 규칙 (Progression Judgment)

매일 문제를 생성하기 전, 사용자가 직전에 채운 `problems/DayNN_*.cpp`를 읽고
현재 주제를 계속할지 / 다음 주제로 넘어갈지 판단한다.

**다음 주제로 넘어가도 좋다고 볼 기준 (모두 충족해야 함):**

- 해당 주제의 모든 변형(예: Stack이면 배열 → 링크드 → 응용)을 다뤘다.
- 마지막 몇 개 문제를 **큰 힌트 없이** 스스로 채웠다.
- 같은 종류의 문법 실수(연산자, 인덱스 경계, STL 사용 등)가
  **반복되지 않는다.**
- 테스트 섹션(`main`)이 통과한다.

> 단순히 "컴파일 되고 테스트 통과" 만으로 완료 처리하지 말 것.
> 문법이 손에 익었는지가 핵심이다.

**완료 처리 절차:**

1. 위 기준을 충족했다고 판단되면, **먼저 사용자에게 물어본다.**
   ("Stack은 충분히 익은 것 같아요. 다음 주제(Queue)로 넘어갈까요?")
2. 사용자가 승인하면, 커리큘럼의 해당 항목을 `[x]`로 표시한다.
3. 사용자가 더 하고 싶어하면, 같은 주제에서 난이도를 한 단계 더 올린 문제를 낸다.

> 완료 여부의 **최종 결정권은 사용자에게 있다.** 임의로 `[x]` 처리하지 말 것.

---

## C++ 스타일 & 환경 (Style & Environment)

- 컴파일러: Homebrew `gcc`가 설치한 버전별 `g++-NN`을, 버전 무관 심볼릭 링크
  `g++cpp`로 고정해서 사용한다 (`ln -sf /opt/homebrew/bin/g++-NN /opt/homebrew/bin/g++cpp`).
  Homebrew가 gcc를 업데이트해 버전 번호가 바뀌어도, 링크만 다시 걸면
  이 문서는 수정할 필요가 없다.
- **모든 명령은 항상 레포 루트(`Coding-Everyday/`)에서 실행한다고 가정한다.**
  경로는 레포 루트 기준 상대경로로 쓰며, 특정 사용자 계정의 절대경로
  (`/Users/xxx/...`)는 절대 쓰지 않는다 — 다른 기기·경로로 옮겨도 깨지지 않게 하기 위함.
- 컴파일 명령 예시 (레포 루트에서 실행, 실행 파일명은 항상 `out`으로 통일):
  `g++cpp -std=c++17 -Wall daily-learning/cpp/problems/Day01_stack_array.cpp -o daily-learning/cpp/problems/out && daily-learning/cpp/problems/out`
- 스타일: 클래스명 `PascalCase`, 함수/변수 `snake_case` 또는 `camelCase` 일관 유지
- STL 학습이 목적이므로, 관련 개념이 나오면 이터레이터·레퍼런스 전달 등
  Python 유사 개념과 비교하는 주석을 markdown 문제 파일에 넣으면 좋다.

---

## 커밋 컨벤션 (Commit Convention)

- `[DS]` — 자료구조 구현 (이 루틴)
- `[PGS]` — 프로그래머스 문제
- `[CF]` — Codeforces 문제

예: `[DS] Day01 Stack 배열 기반 구현`

---

## 하지 말 것 (Do NOT)

- markdown 문제 파일의 "📝 배운 점 / 정리" 섹션을 채우지 말 것. 빈 채로 둔다.
- 스켈레톤에 정답을 그대로 노출하지 말 것. TODO는 반드시 비워 둘 것.
- 테스트 섹션(`main`)의 검증 로직을 사용자가 채울 빈칸으로 만들지 말 것.
- 하루에 여러 자료구조를 몰아서 내지 말 것. 하루 한 주제 원칙.
- 매일 새 폴더를 만들지 말 것. `problems/`·`markdown/`에 파일만 추가한다.
- 기존 `01_stack/`·`02_list/`·`03_stack/` 폴더를 수정·이동하지 말 것.